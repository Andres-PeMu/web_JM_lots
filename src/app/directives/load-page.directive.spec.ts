import { LoadPageDirective } from './load-page.directive';

describe('LoadPageDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadPageDirective();
    expect(directive).toBeTruthy();
  });
});
